#cloud-config
# Minimal Template: Essential secure access configuration for basic system initialization.

# Disable SSH password authentication to prevent brute-force attacks
ssh_pwauth: false

# Define the default user and inject the SSH public key
user:
  name: default
  ssh_authorized_keys:
    - ssh-rsa <YOUR-SSH-PUBLIC-KEY-HERE>
